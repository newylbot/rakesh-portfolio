import {notFound} from "next/navigation";import {siteConfig} from "@/content/site-config";import {createMetadata} from "@/lib/seo";import {ProjectDetail} from "@/components/projects/project-detail";export function generateStaticParams(){return siteConfig.projects.map(p=>({slug:p.slug}))}export function generateMetadata({params}:{params:{slug:string}}){const p=siteConfig.projects.find(x=>x.slug===params.slug);return createMetadata({title:p?.title||"Project",description:p?.oneLine,path:`/projects/${params.slug}`})}export default function Page({params}:{params:{slug:string}}){if(!siteConfig.projects.some(p=>p.slug===params.slug))notFound();return <ProjectDetail slug={params.slug}/>}
